# Use legacy peer deps to handle Tailwind CSS v4 compatibility
# tailwind-scrollbar requires v3 but works fine with v4
legacy-peer-deps=true

# Disable package-lock generation (using pnpm-lock.yaml)
package-lock=false
